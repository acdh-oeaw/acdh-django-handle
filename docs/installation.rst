============
Installation
============

At the command line::

    $ easy_install acdh-django-handle

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv acdh-django-handle
    $ pip install acdh-django-handle
